import { StyleSheet } from "react-native";

const votes = StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems: 'center',
        margin: 10
    },
    purpleText: {
        color: "#B1B2FF",
        fontSize: 17,
    },
})

export { votes }