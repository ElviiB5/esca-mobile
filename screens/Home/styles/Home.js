import { StyleSheet } from "react-native";

const home = StyleSheet.create({
    contactView: {
        flexDirection: "row",
        alignItems: "center"
    },
    contactText: {
        color: "#AAC4FF",
        fontSize: 20,
        marginLeft: 10
    }
})

export { home }