import { StyleSheet } from "react-native";

const home = StyleSheet.create({
    container: {
        marginTop: 40,
        marginBottom: 20
    },
    contactView: {
        flexDirection: "row",
        alignItems: "center"
    },
    contactText: {
        color: "#AAC4FF",
        fontSize: 20,
        marginLeft: 10
    }
})

export { home }